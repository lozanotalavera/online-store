server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lozanotalavera/online-store.git
          searchPath: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}